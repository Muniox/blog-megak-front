@tailwind base;
@tailwind components;
@tailwind utilities;
@layer base {
    html {
        font-family: 'Montserrat', ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
    }
}

input[type="text"]:focus ~ .input-text {
    @apply text-blue-400 bg-white transform top-0 scale-75 -translate-x-5;
}

//* {
//    border: 1px solid gray
//}

Body {
    color: #363636
}

.fix-min-h-screen {
    min-height: 100vh;
}

.fix-min-h-screen {
    min-height: 100svh;
}

.fix-min-h-header {
    min-height: 90vh;
}

.fix-min-h-header {
    min-height: 90svh;
}

.navbar-full {
    min-height: 100lvh;
}

.primary-gradient {
    background: linear-gradient(270deg, #6C63FF -3.81%, #605D94 108.89%);
}

